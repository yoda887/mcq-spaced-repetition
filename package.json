{
  "name": "mcq-spaced-repetition",
  "version": "1.3.0",
  "description": "Spaced repetition plugin focused on multiple choice questions (MCQ).",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && vitest run && node esbuild.config.mjs production",
    "typecheck": "tsc -noEmit -skipLibCheck",
    "version": "node version-bump.mjs && git add manifest.json versions.json",
    "test": "vitest run"
  },
  "keywords": [
    "obsidian",
    "spaced-repetition",
    "flashcards"
  ],
  "author": "Antigravity",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.21.5",
    "jsdom": "^24.1.3",
    "obsidian": "latest",
    "tslib": "^2.6.3",
    "typescript": "^5.4.5",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "ts-fsrs": "^5.4.2"
  }
}
